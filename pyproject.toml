[project]
name = "apology-as-a-service"
version = "0.2.0"
description = "An MCP server that provides apologies as a service for AI agents."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.8.0",
    "uvicorn>=0.30.0",
    "starlette>=0.36.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]
